#include<stdio.h>

int main() 
{
    int a,b,c,d,e,f,g;
    a=33;
    b=20;

      // Addition
    c=a+b;
     printf("Addition = %d\n",c);
      // Subtraction
    d=a-b;
     printf("Subtraction = %d\n",d);
     // Multiplication
    e=a*b;
     printf("Multiplication = %d\n",e);
     // Division
    f=a/b;
     printf("Division = %d\n",f);
     // Modulus
    g=a%b;
     printf("Modulus = %d\n",g);

        return 0;
}